// Generated from content/blog/*.md by scripts/sync-readme.ts.
export const blogPostBodies: Readonly<Record<string, Readonly<{ html: string; headings: readonly Readonly<{ id: string; label: string }>[] }>>> = {
  "introducing-textbutler": {
    "html": "<p>Textbutler is an AI butler for the iMessage, WhatsApp, and Beeper chats you choose on your Mac. It drafts replies, and by default the send decision stays with you.</p>\n<p>A friend asks which weekend works for dinner, your sister wants the name of that plumber, and a former coworker wants to catch up. Each thread needs thirty seconds of thought and a reply you would send, and by Thursday there are dozens. Textbutler runs on your Mac, keeps each contact's context in files you can read, works from the history you choose to import, and writes drafts you check before they go out.</p>\n<p>If you knew it as Message Like Me, Textbutler replaced it; messagelikeme.com now redirects here.</p>\n<h2 id=\"who-it-is-for\">Who it is for</h2>\n<p>Textbutler is for a Mac owner who is behind on personal conversations and wants to read every word the butler writes before it goes out. It also suits developers who already pay for a Claude Code or Codex subscription and want to see a reference application built on xcb.</p>\n<p>It is the wrong tool if you want to message people you do not already talk to, send the same note to many people, or run an assistant that passes itself off as you. Textbutler works only in one-to-one conversations you add one at a time; group chats never start a reply. Its own replies carry a visible marker by default, and we recommend leaving it on.</p>\n<h2 id=\"what-it-does-today\">What it does today</h2>\n<p>Textbutler connects to your messaging apps through Ghostget, which handles sign-in and permissions, so the butler never opens the Messages database itself. You add one conversation at a time. A new contact starts switched off, and a new install starts paused. Adding a conversation never sends a message.</p>\n<p>For each contact, Textbutler keeps a private folder of plain files: who the person is to you, dated notes, and your standing instructions for the butler. Folders are named with opaque identifiers rather than names or phone numbers, and the model can only read and edit files inside the one folder it is working on. Importing history is a separate opt-in step. It takes up to 200 recent messages from that one conversation, keeps who said what and when, records anything it shortened or left out, and does not fetch media. It is a starting sample, not your whole archive.</p>\n<p>The daily loop runs from the terminal:</p>\n<pre><code class=\"language-sh\">textbutler inbox                        # which chats have unanswered messages\ntextbutler replies suggest &lt;contact&gt;    # ask for a draft for one of them\ntextbutler replies show &lt;draft&gt;         # read every line it would send, and to whom\ntextbutler replies send &lt;draft&gt; &lt;digest&gt;\n</code></pre>\n<p><code>inbox</code> only reads. A suggestion is a draft that expires after fifteen minutes and never sends on its own. <code>show</code> prints the exact recipient, every message in order, and a digest of that review. <code>send</code> sends only the draft that matches the digest you just read; if the conversation moved on, your disclosure settings changed, or the draft expired, it refuses instead of sending something stale. You can also type your own reply with <code>replies send &lt;contact&gt; &lt;text&gt;</code>, or <code>discard</code> a draft you do not like. The menu bar companion can check for replies and preview drafts, but a shortened preview there cannot authorize a send.</p>\n<p>When the butler writes, trusted code wraps its text in a marker the other person can see. The default looks like <code>🤖{ hello this is my response }</code>. You can change those markers per contact. You can also clear them, and then the other person sees plain text with nothing to show the butler wrote it, so tell people you use it if you do that. Either way Textbutler records privately which messages it wrote, so its own words never get mixed back in as examples of your style.</p>\n<p>AI drafts run through xcb on the Claude Code or Codex subscription you already have. xcb keeps your sign-in, and the model receives no tools of its own: it returns text or a proposed action, and Textbutler checks it before anything else happens. The details are in <a href=\"/blog/how-textbutler-uses-xcb\">How Textbutler uses xcb</a>.</p>\n<p>To start, clone the repository and open the guided terminal with Bun 1.3.14:</p>\n<pre><code class=\"language-sh\">bun install --frozen-lockfile --ignore-scripts\nbun run textbutler tui\n</code></pre>\n<p>Choose <strong>Setup &amp; readiness</strong>, then <strong>Connect messaging apps</strong>. You can read your inbox and send replies you write yourself without any AI account. AI drafts need a local build (<code>bun run textbutler:install</code>) and a connected xcb account.</p>\n<h2 id=\"where-it-is-going-a-habitat-for-each-conversation\">Where it is going: a habitat for each conversation</h2>\n<p>You write differently to your mother than to your running partner, so one style profile per owner misses most of what matters. Textbutler's longer bet is that each conversation gets its own small, private learning space, which the source calls a habitat.</p>\n<p>Habitats are opt-in and switched on by the owner. Each conversation gets its own, and nothing is shared between them, even between two threads with the same person. You can write a fixed core for a contact, covering the voice the butler should use, your shared history and any boundaries, and the habitat treats it as an anchor it may not rewrite. Over time a background step looks at how past replies landed and proposes a new reply plan: warmer or more direct, more or less formal, longer or shorter, more or less humor. A candidate plan replaces the current one only after a blind side-by-side replay, scored by a separate judge, shows it did no worse on either of the two most recent cases that got a response, and better by a set margin on average. Silence from the other person does not count as success.</p>\n<p>A plan is style and strategy, stored as data. It cannot change who receives a message, which AI provider runs it, what tools are allowed, or whether replies are disclosed. Web search, meme search and the small code sandbox are switches only you control. The learning step is told to leave them alone and keep your core text word for word, and a candidate that changes either one is rejected before it can take over. You can inspect a habitat at any time; rolling it back or clearing what it remembered requires pausing the butler first. <a href=\"/blog/how-textbutler-uses-algal\">How Textbutler uses ALGAL</a> walks through how those programs run.</p>\n<p>Habitats are the direction Textbutler is heading. The code is in the repository and runs only when you turn it on, and like the rest of Textbutler it is in development.</p>\n<h2 id=\"limits-and-status\">Limits and status</h2>\n<p>Current status:</p>\n<blockquote>\n<p>In development. Textbutler runs from source on a Mac; there is no app to download and no published Textbutler package. Without an AI account you can connect iMessage, WhatsApp, and Beeper through Ghostget, check your inbox, and send replies you write yourself. AI replies also need a local build of Textbutler and a Claude Code or Codex subscription connected through xcb. Automatic replies have worked end to end over iMessage in our testing. Try them on your own account, especially over WhatsApp or Beeper, before you rely on them.</p>\n</blockquote>\n<p>Other limits:</p>\n<ul>\n<li><strong>Local does not mean offline.</strong> Textbutler, its files and its send log live on your Mac, and textbutler.app never receives your messages. To write a draft, though, it sends that contact's context to the AI provider you connected. Habitats default to a hosted model route, with a local model server as the alternative, and web search, if you turn it on, sends public queries to a search provider.</li>\n<li><strong>The butler can reply on its own only if you set that up.</strong> For a contact you switch on, after you resume the butler, Textbutler can answer when asked by name or when a classifier is confident help is wanted. Those replies carry the marker unless you cleared it, wait five minutes after you last wrote, are capped per hour, and apply to at most five contacts unless you raise the limit. Disabling the contact or pausing everything cancels pending replies. Leave contacts off and it only drafts.</li>\n<li><strong>History is a sample.</strong> Import is capped, per conversation and without media.</li>\n<li><strong>The Ghostget connection is still being checked.</strong> The live link between Textbutler's background service and Ghostget is waiting on live testing across apps. Beeper reaches apps such as Signal and Telegram as text only, and what is available depends on your linked accounts. Telegram's terms restrict using its content with AI, and connecting through Beeper does not change that. <a href=\"/blog/how-textbutler-uses-ghostget\">How Textbutler uses Ghostget</a> covers what that connection does and does not do.</li>\n<li><strong>No signed app yet.</strong> You build it yourself, and a rebuilt local copy may need macOS permissions granted again.</li>\n</ul>\n<p><a href=\"https://peopleblade.com\">PeopleBlade</a>, a local-first CRM for your personal agent, shares Textbutler's message bundle format: both tools implement the versioned message-like-me.local-message-bundle interchange without importing each other's private state.</p>\n",
    "headings": [
      {
        "id": "who-it-is-for",
        "label": "Who it is for"
      },
      {
        "id": "what-it-does-today",
        "label": "What it does today"
      },
      {
        "id": "where-it-is-going-a-habitat-for-each-conversation",
        "label": "Where it is going: a habitat for each conversation"
      },
      {
        "id": "limits-and-status",
        "label": "Limits and status"
      }
    ]
  },
  "how-textbutler-uses-xcb": {
    "html": "<p>If you already pay for Claude Code or Codex, Textbutler can write your reply drafts on that subscription, and you do not need a second AI plan to get them.</p>\n<h2 id=\"drafting-help-without-another-bill\">Drafting help without another bill</h2>\n<p>A friend asks about Saturday, your landlord wants a time for the repair, and you would like a first draft of each reply. If you already pay for a coding subscription, a separate model plan for drafting would mean paying for AI twice.</p>\n<p>Textbutler hands the writing to xcb, which talks to the subscription you have already signed in to. Your conversations, contacts and sends stay on Textbutler's side.</p>\n<h2 id=\"what-xcb-is-for-someone-who-has-never-used-it\">What xcb is, for someone who has never used it</h2>\n<p><a href=\"https://xcb.sh\">xcb</a>, short for Excalibur, is a local tool that routes coding tasks across the Claude, Codex and Devin subscriptions you already pay for. It keeps the sign-in for each account, starts the provider's program when there is work, and keeps an account locked to one job until that provider process has exited.</p>\n<p>xcb's main interface runs coding tasks. It also has a smaller mode for other applications: an app sends a prompt and gets text back. In that mode xcb gives the model no tools, loads no saved session and runs no hooks or plugins. The model can only answer.</p>\n<p>xcb's documentation names Textbutler as the reference consumer of that mode. Textbutler accepts xcb accounts for Claude Code, Codex and Devin, and a provider still has to pass its checks before any drafting runs on it.</p>\n<p>xcb also decides which accounts an application may use. It offers an account and model to applications only after that exact pair passes xcb's application check, and that approval expires within 24 hours.</p>\n<h2 id=\"how-textbutler-calls-it\">How Textbutler calls it</h2>\n<p>On the Textbutler side, setup happens once, with Textbutler stopped. You point it at the xcb program on your Mac, the folder where xcb keeps its state, one account and one exact model name copied from xcb's own account and model lists:</p>\n<pre><code class=\"language-sh\">bun run textbutler setup \\\n  --xcb /absolute/path/to/xcb \\\n  --xcb-state /absolute/path/to/xcb-state \\\n  --xcb-account claude:ACCOUNT_ID \\\n  --xcb-model FULL_MODEL_KEY\n</code></pre>\n<p>Use <code>codex:ACCOUNT_ID</code> to add a Codex account the same way. Setup records a fingerprint of the xcb program and the account you chose. It does not copy your subscription login, and it does not switch on any contact.</p>\n<p>Every draft then follows the same loop:</p>\n<ol>\n<li><strong>Check the program.</strong> Before each call, Textbutler confirms the xcb program on disk still matches the SHA-256 fingerprint from setup. If you upgraded or replaced it, the call is refused until you review the change.</li>\n<li><strong>Send the prompt privately.</strong> The request, including that contact's context, goes to xcb on standard input, never in command-line arguments where other programs could see it.</li>\n<li><strong>Read the answer strictly.</strong> The reply must be well-formed, name the same account and model that were requested, and fit a size limit. Textbutler rejects JSON with a repeated key, so a reply cannot carry two values for one field.</li>\n<li><strong>Carry out actions itself.</strong> The model may ask for one of a short list of Textbutler actions: edit this contact's notes, read a public web page, or stage a reply. Textbutler checks each request and performs it for that one contact only. A run stops after 16 steps or 12 operations.</li>\n</ol>\n<p>The split of responsibilities:</p>\n<pre><code class=\"language-text\">xcb holds the login and runs the model.\nTextbutler holds the contacts, the recipients and the send button.\n</code></pre>\n<p>The model never gets a file or messaging tool from the provider, and it cannot pick another contact, another recipient or a credential. Textbutler never passes your subscription login to xcb or reads it back.</p>\n<p>AI drafting works only in a local build of Textbutler, made with <code>bun run textbutler:install</code>. The build carries a reviewed record of the exact source files and of Textbutler's two drafting profiles, one to decide whether a message wants a reply and one to write it. If the source has changed since that review, the build refuses. Running Textbutler straight from a source checkout leaves AI drafting switched off.</p>\n<h2 id=\"what-you-get-as-a-user\">What you get as a user</h2>\n<ul>\n<li><strong>Drafts on the plan you have.</strong> Replies are written by the Claude Code or Codex account you connected.</li>\n<li><strong>Your login stays in one place.</strong> xcb keeps the subscription credentials. Textbutler's contact folders never hold them.</li>\n<li><strong>You decide what is sent.</strong> A draft is only text until you read it with <code>replies show</code> and approve it with <code>replies send</code>. Trusted Textbutler code adds the disclosure marker (on by default), checks that the conversation has not moved on, and records the send.</li>\n<li><strong>No quiet switch to paid API use.</strong> If the subscription route is unavailable, busy or out of date, Textbutler reports that and waits. It does not try another account or the Claude API.</li>\n</ul>\n<h2 id=\"the-api-route-is-billed-separately\">The API route is billed separately</h2>\n<p>Textbutler has a second way to draft: the Claude API, using an Anthropic API key you create. Anthropic bills that route per use, apart from any Claude Code subscription. You set it up on purpose: the key goes in a private file, you enter current prices from Anthropic's published pricing, and those price notes expire after 30 days. Each run reserves a conservative amount against a budget you set, $0.25 by default and at most $5. That reservation is a planning limit and does not cap Anthropic's invoice.</p>\n<p>The two routes never mix. Choosing Claude Code or Codex never borrows an API key, and a failed subscription call cannot fall through to API billing. The API route also needs its own trusted packaged runtime, which a source checkout does not provide.</p>\n<h2 id=\"limits\">Limits</h2>\n<p>This is a developer-built integration inside a product that is in development. You install xcb yourself, from its releases or from source, and it must be a build that includes the application mode Textbutler calls. Textbutler pins the exact xcb program you set up rather than a release number. It also pins a companion library, AgentMixer 0.3.0, for shared helpers such as Claude API price parsing; that version number is not the version of xcb. A successful account check reads xcb's account list without making a model call, so it does not prove a draft will be good or a message will arrive. Automatic replies have worked end to end only over iMessage in our testing. Start with an unsent suggestion, and see <a href=\"/blog/introducing-textbutler\">Introducing Textbutler</a> for the full status and the rest of the reply flow. For how xcb itself works, read <a href=\"https://xcb.sh/blog/introducing-xcb\">Introducing xcb</a>.</p>\n",
    "headings": [
      {
        "id": "drafting-help-without-another-bill",
        "label": "Drafting help without another bill"
      },
      {
        "id": "what-xcb-is-for-someone-who-has-never-used-it",
        "label": "What xcb is, for someone who has never used it"
      },
      {
        "id": "how-textbutler-calls-it",
        "label": "How Textbutler calls it"
      },
      {
        "id": "what-you-get-as-a-user",
        "label": "What you get as a user"
      },
      {
        "id": "the-api-route-is-billed-separately",
        "label": "The API route is billed separately"
      },
      {
        "id": "limits",
        "label": "Limits"
      }
    ]
  },
  "how-textbutler-uses-algal": {
    "html": "<p>Textbutler can keep a separate reply plan for each conversation and let it change only when a blinded comparison shows the change made no case worse and helped on average. It runs that learning, which it calls a habitat, as a set of ALGAL programs. Your brother answers in four words and hates small talk; your oldest friend sends paragraphs and expects a joke back. One style for everyone gets at least one of them wrong, and an assistant free to rewrite its own rules could learn something you never wanted.</p>\n<h2 id=\"replies-that-fit-each-person\">Replies that fit each person</h2>\n<p>Textbutler answers as a disclosed assistant, not as you. Someone using it wants those replies to suit the person on the other end and to improve as the conversation goes on. They also need some things to stay fixed. Whatever the assistant learns about tone, it must never learn its way into writing to a different person, switching to a different AI provider, turning on a tool the owner left off, or dropping the marker that shows a reply came from the butler.</p>\n<p>Textbutler separates the part that may change, the reply style, from the part that may not, and runs the changing part inside programs that record each run.</p>\n<h2 id=\"what-algal-does\">What ALGAL does</h2>\n<p><a href=\"https://algal.computer\">ALGAL</a> is a language and application VM for agent programs that run within set limits. An ALGAL program, which ALGAL calls an organism, is written as data: a list of steps, how they connect, and the limits each step must respect, such as how much context a model may read, how long a step may take, and how many model calls the whole run may make. The host application decides which tools exist and what is allowed, and the program cannot grant itself more.</p>\n<p>Each run leaves a record of every step and its result, so someone can see later what happened without calling the model again. ALGAL's README states its larger bet, that a computer can keep tested ways of acting and get better at later work, and calls whether this beats equally resourced alternatives an open question.</p>\n<h2 id=\"how-textbutler-runs-a-habitat-on-algal\">How Textbutler runs a habitat on ALGAL</h2>\n<p>Textbutler is In development and runs from source. Habitats are an opt-in part of it and stay off unless the owner turns them on in Textbutler's host settings. Once they are on, each enrolled conversation gets its own habitat, stored in Textbutler's private journal on the Mac. Habitats share nothing, even two threads with the same person.</p>\n<p>A habitat does its model work in three kinds of runs. Each is a small ALGAL program with a single model step and limits fixed in code:</p>\n<ul>\n<li><strong>Respond</strong> decides whether a reply is wanted and, if so, proposes one for this conversation. It gets up to 32 KiB of context and 25 seconds. When the plan allows a tool such as meme or memory search, a reply can take up to three respond runs, with the host running the tool in between.</li>\n<li><strong>Reflect</strong> looks at a reply that went out and what the contact said afterward, then proposes a revised plan. It gets up to 96 KiB of context and two minutes.</li>\n<li><strong>Judge</strong> scores the current plan and the candidate plan side by side on the same past cases. It has the same limits as reflect.</li>\n</ul>\n<p>Every run allows at most one model call and four steps. Reflect and judge use the Claude Code subscription you connect through xcb, with no tools at all: the model returns JSON and Textbutler checks it. Learning runs only when the owner has also named a model for it in the habitat settings. Those calls are not cached or retried, so a failed learning step keeps the current plan instead of running twice.</p>\n<h3 id=\"a-plan-is-data-with-a-short-list-of-fields\">A plan is data with a short list of fields</h3>\n<p>What learns is the contact's plan, which holds style and strategy and nothing else. A plan looks like this:</p>\n<pre><code class=\"language-json\">{\n  &quot;version&quot;: 1,\n  &quot;guidance&quot;: &quot;Short answers. Ask before making plans for both of us.&quot;,\n  &quot;personality&quot;: { &quot;tone&quot;: &quot;warm&quot;, &quot;formality&quot;: &quot;casual&quot; },\n  &quot;humor&quot;: &quot;light&quot;,\n  &quot;contextMessages&quot;: 12,\n  &quot;maxReplyCharacters&quot;: 400,\n  &quot;webSearch&quot;: false,\n  &quot;memeSearch&quot;: true,\n  &quot;javascript&quot;: false,\n  &quot;memorySearch&quot;: true\n}\n</code></pre>\n<p>Tone is one of neutral, warm, playful or direct. Formality is casual, balanced or formal. Reply length and context size have fixed minimums and maximums. The schema is strict, so a plan with any other field is rejected before it is used.</p>\n<p>The main guarantee comes from that strictness. The plan has no field for the recipient, the AI provider, permissions or disclosure, so no plan, learned or hand-written, can express a change to them. The program's instructions say the same in words, and the plan reaches the model labeled as untrusted strategy data, separate from the host's instructions.</p>\n<p>The owner can also write a fixed core for a contact: the voice to use, relationship context, shared history and boundaries. The learning step must keep that core word for word and leave the owner's tool switches (web search, meme search, JavaScript and memory search) alone.</p>\n<h3 id=\"when-a-new-plan-replaces-the-old-one\">When a new plan replaces the old one</h3>\n<p>After a reply goes out and the contact responds, the reflect run may propose a candidate plan. Textbutler then reruns the respond program with each plan on the two most recent past cases that drew a follow-up. It sets which answer appears first from a hash of the case, so the judge cannot rely on position, and asks the judge to score the anonymized pair. The candidate replaces the current plan only when all of these hold:</p>\n<pre><code class=\"language-text\">the owner has not edited the habitat or its memory since the comparison started\nthe candidate differs from the current plan and is not one the owner rolled back\nthe candidate cites at least one real follow-up message from each case\nthe judge marks the candidate safe on every case\ncandidate score &gt;= current score on every case\naverage(candidate - current) &gt;= 0.1\ntool switches and the owner's core text are unchanged\n</code></pre>\n<p>If any line fails, the current plan stays. Silence from the contact counts as unknown, and the instructions tell the reflect step not to optimize for dependency, message volume, provocation or guilt. When a candidate wins, the previous plan is kept as an ancestor so the owner can roll back to it. An evaluation that fails partway leaves the current plan in place and is not retried.</p>\n<p>Every habitat run, from live replies to reflections, replays and judge runs, leaves an ALGAL record in Textbutler's private journal. Each evaluation stores the digests of the runs behind it, and Textbutler keeps the full records of the most recent 32 runs per contact, so you can trace a recent plan change back to the runs that produced it.</p>\n<h2 id=\"what-changes-for-a-textbutler-user\">What changes for a Textbutler user</h2>\n<p>Each conversation gets a reply plan that can move toward what works with that person, one checked step at a time, with a record of why recent changes happened. Learning starts only after a reply was sent, so a draft you threw away never becomes a lesson.</p>\n<p>The owner controls the plan. <code>show</code> works at any time. The other three commands require automatic replies to be paused and take the habitat revision that <code>show</code> reports, so they fail if the habitat changed in the meantime:</p>\n<pre><code class=\"language-sh\">textbutler habitats show &lt;contact&gt;                         # plan, memory, learning history and budget\ntextbutler habitats configure &lt;contact&gt; &lt;revision&gt; &lt;json&gt;  # set a starting plan\ntextbutler habitats rollback &lt;contact&gt; &lt;revision&gt;          # return to the previous plan\ntextbutler habitats memory-clear &lt;contact&gt; &lt;revision&gt;      # clear learned excerpts\n</code></pre>\n<p>A rollback steps back one saved plan at a time and cannot reach past the owner's last <code>configure</code>, so it never brings back a tool the owner switched off. Clearing memory empties the learned excerpts, and older run records stay in the journal.</p>\n<p>A habitat never decides who receives a message and has no way to send one. The model returns proposed actions, such as a text or a reaction, and Textbutler checks them against what the conversation allows. Whether and how anything is sent stays with Textbutler's normal send path and your settings for that contact, which <a href=\"/blog/introducing-textbutler\">Introducing Textbutler</a> walks through.</p>\n<h2 id=\"what-the-comparison-does-not-measure\">What the comparison does not measure</h2>\n<p>Replays generate text only and run no tools, so the rule does not measure whether a candidate would choose better searches or memes. Two past cases are a small sample, and the judge is itself a model reading untrusted text. No published measurement yet shows that habitats make replies better for real contacts over time.</p>\n<p>Habitat runs send conversation context to a model. Replies default to a hosted Qwen model through Vercel AI Gateway, capped by a daily budget you set, or go to a local OpenAI-compatible model server if you choose one. Learning runs through the Claude Code subscription you connect with xcb.</p>\n<p>Other products that run on ALGAL are listed on <a href=\"https://algal.computer/blog/built-on-algal/\">Built on ALGAL</a>.</p>\n",
    "headings": [
      {
        "id": "replies-that-fit-each-person",
        "label": "Replies that fit each person"
      },
      {
        "id": "what-algal-does",
        "label": "What ALGAL does"
      },
      {
        "id": "how-textbutler-runs-a-habitat-on-algal",
        "label": "How Textbutler runs a habitat on ALGAL"
      },
      {
        "id": "what-changes-for-a-textbutler-user",
        "label": "What changes for a Textbutler user"
      },
      {
        "id": "what-the-comparison-does-not-measure",
        "label": "What the comparison does not measure"
      }
    ]
  },
  "how-textbutler-uses-ghostget": {
    "html": "<p>Textbutler can import your Beeper and WhatsApp history as context for each conversation, but it never signs in to either service and holds no Beeper or WhatsApp password or session. Ghostget, a separate local tool, exports that history to a private folder in Textbutler's own format. Textbutler then checks the folder and imports it on your Mac. The two programs do not talk to each other during the import; the folder is the whole handoff. (Textbutler's live inbox and replies also go through Ghostget, but that is a separate path this post does not cover.)</p>\n<h2 id=\"who-this-is-for\">Who this is for</h2>\n<p>You already use Beeper, WhatsApp or both, and you want the butler to have the history of a conversation with a friend, a sibling or a group chat. You want that without giving a third app your messaging logins, and without anything leaving your Mac.</p>\n<p>The export covers your own history, from accounts you are signed in to. It also contains the other people's messages, since that is what a conversation is.</p>\n<h2 id=\"what-ghostget-does\">What Ghostget does</h2>\n<p><a href=\"https://ghostget.com\">Ghostget</a> is a local command line tool that works with the accounts you are already signed in to on your own computer. For messaging, it can read your Beeper Desktop history and your WhatsApp linked-device history. Ghostget holds the sign-in state and runs the export, and Textbutler never sees that state.</p>\n<p>Here Ghostget writes a finished folder of plain text files that describe your accounts, the people in each conversation, the conversations, the messages, reactions, and deletions.</p>\n<pre><code class=\"language-sh\">ghostget beeper export-message-like-me --auth &lt;your-beeper-auth&gt; \\\n  --output /absolute/private/path/beeper-bundle --json\n\nghostget whatsapp export-message-like-me --auth &lt;your-whatsapp-auth&gt; \\\n  --output /absolute/private/path/whatsapp-bundle --json\n</code></pre>\n<p>The command names still carry Message Like Me, Textbutler's earlier name, because the format was named then and existing tools depend on it.</p>\n<h2 id=\"how-textbutler-uses-it\">How Textbutler uses it</h2>\n<p>Textbutler's side is one import command, run with its <code>messagelikeme</code> command-line tool, which also keeps the earlier name:</p>\n<pre><code class=\"language-sh\">messagelikeme ingest bundle --input /absolute/private/path/beeper-bundle --json\nmessagelikeme sources list --json\n</code></pre>\n<p>The import reads only the finished folder. It does not start Ghostget, open Beeper or WhatsApp, use the network, or send anything.</p>\n<h3 id=\"textbutler-defines-the-format\">Textbutler defines the format</h3>\n<p>The folder format is defined in Textbutler's repository, as a small module of types and strict checking functions that does no file, network or messaging work of its own. Ghostget does not keep its own copy of those rules. It depends on Textbutler's module, pinned to one fixed commit, and runs every record it writes through Textbutler's own checks before the folder is finished.</p>\n<p>There are two versions:</p>\n<ul>\n<li><strong>Version 1, for Beeper.</strong> One folder can hold several connected accounts, because Beeper bridges several networks. Each account becomes its own source in Textbutler.</li>\n<li><strong>Version 2, for WhatsApp.</strong> One folder holds exactly one WhatsApp account, and every address must be a well-formed WhatsApp identifier. Status updates, broadcasts and newsletters are rejected.</li>\n</ul>\n<p>Because Ghostget checks its output with Textbutler's code at a fixed commit, the rules change in one place, and Ghostget adopts a change only when its pin is updated.</p>\n<h3 id=\"each-folder-lists-hashes-for-its-own-files\">Each folder lists hashes for its own files</h3>\n<p>A bundle is seven files: six line-per-record text files and an index file written last. The index lists each file's record count, byte length and SHA-256 hash, plus one hash over the index itself, computed like this:</p>\n<pre><code class=\"language-text\">bundle hash = SHA-256( canonical JSON of the index, without its integrity section )\n</code></pre>\n<p>&quot;Canonical&quot; means one exact spelling of the JSON, so the same content always produces the same bytes and the same hash. Textbutler recomputes all of it before it changes anything in its store. It also refuses a folder that is not private to your user account, contains an extra file, a symbolic link or a hard link, or changes while it is being read. Checking finishes before the store is touched, and the import itself is one database transaction, so a folder that fails is not partly imported.</p>\n<h3 id=\"both-repositories-test-the-same-sample-folder\">Both repositories test the same sample folder</h3>\n<p>A small synthetic bundle, with invented accounts such as &quot;Synthetic Primary&quot; and a placeholder phone number, is checked into both repositories as identical files:</p>\n<ul>\n<li>Ghostget's tests run its real exporter over the synthetic source with a fixed clock and require the output to match the checked-in folder byte for byte.</li>\n<li>Textbutler's tests import that same checked-in folder and require the index file's SHA-256 to match the value recorded in the test.</li>\n</ul>\n<p>If Ghostget's output changes by one byte, its own test fails. If Textbutler's importer stops accepting the checked-in folder, Textbutler's test fails. The two copies are kept identical by hand rather than by a shared check, so a deliberate format change means regenerating the folder in Ghostget and copying it into Textbutler in step.</p>\n<h3 id=\"re-importing-does-not-erase-history\">Re-importing does not erase history</h3>\n<p>Each export is a snapshot of what your Mac could see at that moment, and Textbutler treats it that way:</p>\n<ul>\n<li>A later export that leaves out an older message does not delete it from Textbutler.</li>\n<li>An explicit deletion record hides its target, and if the message reappears later it comes back.</li>\n<li>An older snapshot cannot overwrite newer state.</li>\n</ul>\n<p>You can run the export again next month, and a smaller window will not throw away what you imported before.</p>\n<h2 id=\"what-you-get\">What you get</h2>\n<p>Your Beeper and WhatsApp conversations land in Textbutler's private store on your Mac, where its replies can draw on that history, and Textbutler never holds a Beeper or WhatsApp login. <code>sources list</code> shows what was imported, and <code>sources show</code> reports each source's health.</p>\n<p>If you use WhatsApp both natively and through Beeper, Textbutler stops and asks you to name the overlapping Beeper source with <code>--overlap-source</code> before it imports the native export. Both sources stay stored. It counts two messages as one only in one-to-one chats where your own number and the other person's number match exactly, and only after at least one unambiguous shared message agrees on sender, time, direction, text and kind. Names, partial numbers, approximate times, group chats and messages without text never count as a match.</p>\n<h2 id=\"limits\">Limits</h2>\n<ul>\n<li><strong>It is not your whole history.</strong> Each export records what the local apps had on your Mac, and says so. The Beeper export marks that it does not claim remote history, and the WhatsApp export marks remote history as incomplete.</li>\n<li><strong>No media.</strong> Attachments come across as names and types only. Images, audio and video stay where they are.</li>\n<li><strong>WhatsApp reactions are left out.</strong> The WhatsApp tool Ghostget reads cannot tell whether a reaction was later removed, so Ghostget drops reaction rows and adds a warning when it saw any. An empty reactions file in a WhatsApp bundle means reactions could not be observed.</li>\n<li><strong>The hashes cover the folder only.</strong> They show the folder was not damaged or edited after Ghostget wrote it. They say nothing about whether the messaging app's data was right or complete.</li>\n<li><strong>Keep the folder private.</strong> It is not anonymized. Names, phone numbers, message text and who talks to whom are all in it. Do not put it in Git, a shared folder or a cloud drive.</li>\n</ul>\n<p>Textbutler is In development. It runs from source on a Mac and has no downloadable app yet; <a href=\"/blog/introducing-textbutler\">Introducing Textbutler</a> covers what it does today and how to start. Ghostget lists the products that use it on <a href=\"https://ghostget.com/blog/built-on-ghostget\">Built on Ghostget</a>.</p>\n",
    "headings": [
      {
        "id": "who-this-is-for",
        "label": "Who this is for"
      },
      {
        "id": "what-ghostget-does",
        "label": "What Ghostget does"
      },
      {
        "id": "how-textbutler-uses-it",
        "label": "How Textbutler uses it"
      },
      {
        "id": "what-you-get",
        "label": "What you get"
      },
      {
        "id": "limits",
        "label": "Limits"
      }
    ]
  }
};
